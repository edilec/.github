# Repository preview assets

`source/community-defaults-social-preview.svg` is the editable 1280 × 640
source. `dist/community-defaults-social-preview.png` is the optimized upload
for GitHub's repository social-preview setting.

The artwork describes the repository's actual role: it provides account-wide
community defaults while project-specific guidance remains authoritative.
